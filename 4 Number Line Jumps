 if (v1 != v2) {
            int d = x2 - x1;
            int v = v1 - v2;
           
            if (d % v == 0 && d / v >= 0) {
                return "YES";
            }
        } else if (x1 == x2) {
            return "YES";
        }
        return "NO";
    }
